<div
    class="shortcode-why-choose-us"
    data-title="{{ $title }}"
    data-subtitle="{{ $subtitle }}"
    data-title-color="{{ $title_color }}"
    data-subtitle-color="{{ $subtitle_color }}"
    data-tabs="{{ json_encode($tabs) }}"
></div>

